# ArduPilot SITL + FOLLOW_EXT 控制台使用说明

本文用于说明如何启动 ArduPilot SITL，并使用 `rt_send.py` 控制自定义飞行模式。

## 1. 准备环境

- 已编译包含自定义串口协议和飞行模式的 ArduPilot 代码。
- 已安装 Python 3 和 Tkinter。
- SITL 将自定义串口协议映射到 TCP：`127.0.0.1:5762`。

## 2. 启动 ArduPilot SITL

进入 ArduPilot 根目录，启动 Copter SITL：

```bash
cd ~/ardupilot
python3 Tools/autotest/sim_vehicle.py -v ArduCopter -f quad --console --map
```

如果你的自定义串口需要在启动参数中映射到 TCP，使用项目实际对应的 UART 编号。例如：

```bash
python3 Tools/autotest/sim_vehicle.py -v ArduCopter -f quad \
  --console --map --serial0=uart:tcp:127.0.0.1:5762
```

如果自定义协议使用的是 `SERIAL1`，将 `--serial0` 换成 `--serial1`。具体 UART 编号以飞控代码配置为准。

确认 TCP 端口已经监听：

```bash
ss -ltn | grep 5762
```

看到 `127.0.0.1:5762` 处于监听状态后，再启动控制软件。

> `127.0.0.1:5762` 是本工具使用的自定义串口协议端口，不是 MAVLink 端口。不要仅使用 `--out` 代替自定义串口映射。

## 3. 启动控制软件

打开新的终端，进入本项目目录：

```bash
cd /home/shuwei/myrepo
python3 rt_send.py --ip 127.0.0.1 --port 5762 --log follow_ext.log
```

软件启动后会自动连接，也可以在左上角输入 IP、端口后点击“连接”。

连接成功后，软件会自动对协议 5.3 中的 7 个 FOLLOW_EXT 参数发送 GET，读取飞控当前值。

## 4. 基本操作

1. 在“控制帧字段 (0x01)”中设置目标高度、速度、目标位置和 XYZ 偏移。这些字段属于周期发送的飞行控制帧，不是参数设置。
2. 点击“起飞”，发送 `0x04` 起飞模式。
3. 点击“导航”，发送 `0x03` GPS 位置模式。
4. 点击“视觉”，发送 `0x01` 视觉模式。
5. 在右侧视觉区域点击或拖动鼠标，设置模拟目标位置。Y/Z 偏移会随 20 Hz 控制帧持续发送。
6. 点击“悬停”可发送 `0x02` 悬停模式。
7. 点击“降落”后确认。协议没有独立降落指令，软件会发送 GPS 位置模式，并将目标高度设为 `0 cm`。
8. 仿真结束前建议先点击“悬停”或“降落”，再断开软件和关闭 SITL。

FOLLOW_EXT 参数显示在“参数设置”标签页的“FOLLOW_EXT 参数 (协议 5.3)”区域。先用单选按钮选中一个参数，再点击对应操作按钮：

- “读取选中”发送 GET。
- “临时写入选中”发送 `SET_VOLATILE`，重启后恢复。
- “持久化写入选中”发送 `SET_PERSISTENT`，保存到飞控参数存储。


## 5. 常见问题

- **连接失败**：确认 SITL 已启动，并且 `5762` 确实是自定义协议的 TCP 端口。
- **已连接但没有状态反馈**：检查飞控是否按协议返回 FCU 状态帧，以及自定义协议使用的 UART 编号。
- **无法进入视觉模式**：读取或设置 `FOLE_AUTO_ENABLE=1`，然后重新点击“视觉”。
- **参数单位**：高度单位为 cm，经纬度为度乘 `1e7`，目标速度为 `0.01 m/s`。

本工具仅用于 ArduPilot SITL 仿真和协议调试。
