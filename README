用来将ardupilot仿真的串口以tcp的形式接出来，方便调试
使用方法：
ArduPilot TCP 双向数据工具使用说明

简介

这个工具用于与 ArduPilot SITL 或其他 TCP 服务器进行双向数据通信，支持自动发送、接收和交互式发送模式。

安装

1. 确保已安装 Python 3.x
2. 无需额外安装，工具使用标准库

基本用法


python ardu_tcp_bridge.py [选项]


选项说明

选项 说明

--ip TCP服务器IP地址 (默认: 127.0.0.1)

--port TCP服务器端口 (默认: 9998)

--send HEX_DATA 要发送的十六进制数据 (例如: "B5 62 01 07 3C 00")

--rate 发送速率 (Hz) (仅重复模式有效, 默认: 1.0)

--repeat 重复发送数据 (默认: 单次发送)

--log FILE 记录所有收发数据到指定文件

使用示例

1. 简单连接并交互式发送数据

   python ardu_tcp_bridge.py --ip 127.0.0.1 --port 9998
   
   进入交互模式后，可以输入十六进制数据发送

2. 自动发送特定数据

   python ardu_tcp_bridge.py --send "B5 62 01 07 3C 00"
   

3. 重复发送数据

   python ardu_tcp_bridge.py --send "B5 62 01 07 3C 00" --repeat --rate 10
   
   以10Hz的频率重复发送数据

4. 记录通信日志

   python ardu_tcp_bridge.py --log comm.log
   

交互模式命令

在交互模式下，支持以下命令：

• exit/quit - 退出交互模式

• status - 显示连接状态

• help - 显示帮助信息

十六进制数据格式

可以输入以下格式的十六进制数据：
• 带空格分隔：B5 62 01 07 3C 00

• 连续格式：B56201073C00

• 混合格式：B5 62 01:07,3C00

注意事项

1. 确保目标TCP服务器已启动并监听指定端口
2. 发送速率过高可能导致数据丢失或连接问题
3. 日志文件会记录所有收发数据的十六进制格式
4. 使用Ctrl+C可以中断程序运行

典型应用场景

1. 测试ArduPilot SITL的TCP接口
2. 模拟传感器数据发送
3. 调试通信协议
4. 记录和分析通信数据

输出说明

• 发送和接收的数据会显示：

  • 数据长度（字节数）

  • 十六进制格式

  • ASCII可打印字符（非打印字符显示为点号）

• 连接状态变化会实时显示

• 错误信息会明确提示原因